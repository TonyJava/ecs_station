#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from port_server import port_server
from port_server.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  string exc(string cmd)'
  print '  void exc_without_return(string cmd)'
  print '  string set_serial_port(string port, i32 rate)'
  print '  void send_cmd(string code)'
  print '  string get_var(string key)'
  print '  string get_var_list(string key)'
  print '  string get_var_table()'
  print '  string set_list_len(i32 len)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = port_server.Client(protocol)
transport.open()

if cmd == 'exc':
  if len(args) != 1:
    print 'exc requires 1 args'
    sys.exit(1)
  pp.pprint(client.exc(args[0],))

elif cmd == 'exc_without_return':
  if len(args) != 1:
    print 'exc_without_return requires 1 args'
    sys.exit(1)
  pp.pprint(client.exc_without_return(args[0],))

elif cmd == 'set_serial_port':
  if len(args) != 2:
    print 'set_serial_port requires 2 args'
    sys.exit(1)
  pp.pprint(client.set_serial_port(args[0],eval(args[1]),))

elif cmd == 'send_cmd':
  if len(args) != 1:
    print 'send_cmd requires 1 args'
    sys.exit(1)
  pp.pprint(client.send_cmd(args[0],))

elif cmd == 'get_var':
  if len(args) != 1:
    print 'get_var requires 1 args'
    sys.exit(1)
  pp.pprint(client.get_var(args[0],))

elif cmd == 'get_var_list':
  if len(args) != 1:
    print 'get_var_list requires 1 args'
    sys.exit(1)
  pp.pprint(client.get_var_list(args[0],))

elif cmd == 'get_var_table':
  if len(args) != 0:
    print 'get_var_table requires 0 args'
    sys.exit(1)
  pp.pprint(client.get_var_table())

elif cmd == 'set_list_len':
  if len(args) != 1:
    print 'set_list_len requires 1 args'
    sys.exit(1)
  pp.pprint(client.set_list_len(eval(args[0]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
